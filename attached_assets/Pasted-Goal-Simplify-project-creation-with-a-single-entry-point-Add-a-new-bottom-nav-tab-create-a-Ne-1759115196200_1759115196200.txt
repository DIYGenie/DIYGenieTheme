Goal: Simplify project creation with a single entry point. Add a new bottom nav tab (+), create a New Project form, and replace old CTAs with one big amber-outline button.

Bottom nav update

Add a center nav tab with a “+” icon (lucide-react or expo icon set).

Label: New Project.

Tapping opens a new screen: app/NewProjectForm.tsx.

New Project form (NewProjectForm.tsx)

Layout: white background, vertical stack, padding 24.

Title: “Start a New Project” (Manrope 22/700, slate-900).

Subtitle: “Tell us what you’d like DIY Genie to help you build” (Inter 14/500, slate-600).

Fields:

TextArea (multiline): placeholder “e.g. Build 3 floating shelves for living room wall”.

Dropdown 1: Budget → options [$, $$, $$$].

Dropdown 2: Skill Level → options [Beginner, Intermediate, Advanced].

Continue button (medium rounded-rect, amber fill, white text).

Disabled until all fields are filled.

On press → navigate to NewProjectMedia.tsx.

Media selection screen (NewProjectMedia.tsx)

Title: “Add Room Photo”.

Subtitle: “Scan your room or upload a photo to begin.”

Two square tiles (same style as old CTAs, 120×120, radius 16, shadow soft):

Scan Room: outlined amber (white bg, amber border, amber icon/text).

Upload Photo: neutral (white bg, gray border, slate icon/text).

Back button to return to form.

Home screen cleanup

Remove the two square CTAs.

Replace with one large button under welcome message:

Label: “Start Your First Project”.

Style: outlined amber (bg white, 2px amber border, amber text).

Width ~70%, height ~52, radius 16, centered.

On press → navigate to NewProjectForm.

Projects screen cleanup

Remove CTAs at top.

If no projects exist → show empty state card with illustration + the same “Start Your First Project” button (outlined amber).

If projects exist → button not shown (just list).

Deliver

Show updated bottom nav with + tab.

Show Home with single big button.

Show NewProjectForm (fields + continue button).

Show NewProjectMedia (Scan Room + Upload Photo).