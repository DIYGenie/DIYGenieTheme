Goal: The entire New Project screen fits on one page without scrolling. Keep everything on a single screen by reducing vertical padding, input height, and tile size, while preserving usability.

1) Layout: no scroll, space-between

Replace the scroll container with a SafeArea layout:

import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { useBottomTabBarHeight } from '@react-navigation/bottom-tabs';
import { useWindowDimensions } from 'react-native';

const insets = useSafeAreaInsets();
const tabBarHeight = useBottomTabBarHeight();
const { height: H } = useWindowDimensions();
const isSmall = H < 740; // e.g., iPhone 11/12/13 in portrait threshold


Screen structure:

<SafeAreaView style={{ flex: 1, backgroundColor: '#FFF' }}>
  <View style={{ flex: 1, paddingHorizontal: 20, paddingTop: 6, paddingBottom: tabBarHeight + insets.bottom + 8, justifyContent: 'space-between' }}>
    {/* Top: title + form */}
    {/* Bottom: media section with stacked tiles */}
  </View>
</SafeAreaView>

2) Compact top section (title + form)

Title/subtitle spacing:

Title: Manrope 22/700

Subtitle: Inter 13/500, color slate-600, marginTop: 4, marginBottom: 12

Inputs:

Input height 44

Corner radius 12

Vertical gaps between fields 12

Keep helper text “x/10 minimum” but set fontSize: 12 and marginTop: 6

Dropdowns stay Portal-based (so they overlay), with menu container zIndex: 1000, elevation: 1000. Field wrappers:

budgetWrapper: { position: 'relative', zIndex: 30, elevation: 30 }
skillWrapper:  { position: 'relative', zIndex: 20, elevation: 20 }

3) Bottom section: stacked square tiles (compact)

Section header with soft divider, tighter:

<View style={{ marginTop: isSmall ? 12 : 16, marginBottom: isSmall ? 8 : 10, alignItems: 'center' }}>
  <View style={{ height: 1, width: '100%', backgroundColor: 'rgba(229,231,235,0.9)' }} />
  <Text style={{ position: 'absolute', top: -9, paddingHorizontal: 8, backgroundColor: '#FFF', color: '#6B7280', fontSize: 12, fontWeight: '600' }}>
    Add your room photo
  </Text>
</View>


Tile sizes responsive (keeps all on one page):

const TILE = isSmall ? 92 : 104;    // min touch > 44px is preserved
const GAP  = isSmall ? 10 : 12;


Tiles container:

<View style={{ alignItems: 'center', gap: GAP }}>
  {/* Scan Room tile */}
  {/* Upload Photo tile */}
</View>


Tile style (shared):

{
  width: TILE, height: TILE, borderRadius: 16,
  justifyContent: 'center', alignItems: 'center',
  backgroundColor: '#FFF',
  shadowColor: '#000', shadowOpacity: 0.06, shadowRadius: 12,
  shadowOffset: { width: 0, height: 4 }, elevation: 6,
}


Scan Room: borderWidth: 1.5, borderColor: '#FBBF24', icon/text #F59E0B

Upload Photo: borderWidth: 1, borderColor: '#E5E7EB', icon/text #1F2937'

Disabled state: opacity: 0.6, shadowOpacity: 0

Icon size 22 (isSmall) / 24 (default), label 13 (semi-bold)

4) Validation (unchanged)

Tiles enabled only when:

description length ≥ 10

budget selected

skill level selected

5) Tab bar clearance (no overlap)

The outer container already has paddingBottom: tabBarHeight + insets.bottom + 8.

Ensure no sticky footers exist; tiles are in normal flow so the page height compresses correctly.

6) Deliver

Show New Project on a common device frame (≈812 height) with no scrolling.

Both tiles fully visible above the gradient tab bar.

Open the Skill/Budget dropdowns to verify they overlay cleanly.

If a very small device is detected (height < 680), reduce TILE to 88 and label to 12 so it still fits.